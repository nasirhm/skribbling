# NetBSD Rev Shell

Small helper script to run on rvb competitions to spawn shells over tcp sockets.

For NetBSD, we have the compiled executable in `/etc/sbin`.

Hence in the rc script, we use it with port 5000.
